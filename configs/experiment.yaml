# configs/experiment.yaml
# Default experiment configuration.
# CLI args override individual fields: --domain, --layout, --scenario, --steps, --planner, --recognizer

domain: kitting
layout: env_layout1
scenario: scenario_10
steps: 30
planner: basic
recognizer: uniform_make_domain_model